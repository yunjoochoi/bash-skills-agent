# bash-skills-agent

Google ADK-based agent application

## Setup

```bash
uv venv && source .venv/bin/activate
uv sync
cp .env.example .env  # Edit with your settings
```

## Run

```bash
adk web src/
```
